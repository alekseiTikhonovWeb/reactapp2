services:
  app:
    build:
      context: ./MyDockerApp
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./MyDockerApp:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
